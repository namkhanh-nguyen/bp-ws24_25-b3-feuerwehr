import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
      domains: ['res.cloudinary.com'], // Füge hier die benötigte Domain hinzu
    },
};

export default nextConfig;
